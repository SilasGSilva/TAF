import { LaborProcessComponent } from './labor-process.component';

describe('LaborProcessComponent', () => {
  let component: LaborProcessComponent;

  beforeEach(() => {
    component = new LaborProcessComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
